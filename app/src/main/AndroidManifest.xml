<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.parkly.countrycodepickerproject">

    <application
        android:icon="@drawable/ic_ccp_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="io.parkly.countrycodepickerproject.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="io.parkly.countrycodepickerproject.ExampleActivity"
            android:windowSoftInputMode="stateHidden"
            android:exported="false"/>
    </application>

</manifest>
